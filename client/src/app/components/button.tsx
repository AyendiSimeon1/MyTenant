// import React from 'react';
// import { useDispatch } from 'react-redux';
// import { someAction } from '../redux/actions'; // Import your action

// const Button = ({ children, onClick, type, ...props }) => {
//   const dispatch = useDispatch();

//   const handleClick = () => {
//     dispatch(someAction(data)); // Dispatch action with data (if needed)
//     if (onClick) onClick(); // Call additional onClick handler if provided
//   };

//   return (
//     <button type={type} onClick={handleClick} {...props}>
//       {children}
//     </button>
//   );
// };

// export default Button;